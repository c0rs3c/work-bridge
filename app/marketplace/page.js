import SiteShell from "@/components/site-shell";

export default function MarketplacePage() {
  return (
    <SiteShell title="Marketplace (Phase 2)">
      <div className="card">
        <p className="text-sm text-muted">Marketplace listings and matching will be added in Phase 2.</p>
      </div>
    </SiteShell>
  );
}
